# AuditAI Busan — 사용 안내 (실무)

## 실행

**`시작하기.bat`** 더블클릭 → 주소가 `http://127.0.0.1:8765` 인지 확인합니다.  
HTML 파일을 직접 열면(AI·일부 저장) 불안정합니다.

### AI (선택)
- `.env`에 `OPENAI_API_KEY=`만 채웁니다.
- 상담 후 **AI 해설 요청** — 판정은 규칙엔진이 유지합니다.
- 증빙요청에서 **AI로 자연스럽게** — 메일 문장만 다듬고 서류 목록은 그대로입니다.

---

## 하루 업무 (권장 순서)

### 1. 문서등록
- **① 지침·매뉴얼·체크리스트** (PDF·TXT) — 검색·답변용
- **② 증빙 서식** (HWP·PDF·DOCX…) — 메일 첨부용
- 사업을 여러 개 쓰면 상단에서 전환합니다.

### 2. 상담
- **간략한 예산 설명**에 “무엇을·얼마에·왜”만 적어도 됩니다. 정확한 비목을 몰라도 됩니다.
- 설명이 비목을 **자동 추정**하고, **그 비목에 필요한 입력만** 열립니다 (회의→인원·시각, 물품·용역→계약상대·쪼개기 등).
- 틀리면 추정 비목 셀렉트·후보를 고칩니다. 자주 쓰는 유형은 위 칩으로 채울 수 있습니다.
- **답변 생성** → 가능여부·증빙 + **수의계약 쪼개기(분할발주) 점검** → 문의 건 자동 저장 (**일련번호** `C-연도-순번` 부여)
- 앱을 다시 열면 **진행 중 건 이어서 하기** 창이 뜹니다. 일련번호로 찾아 이어갈 수 있습니다.
- 다른 건을 쓰려면 반드시 **새 문의**

### 3. 증빙요청
- 요청할 서류만 **체크** (해제 = 메일·ZIP·접수에서 제외)
- 목록에 없으면 **서류 추가**
- **전체 요청 초안 / 발송** — 필요 서류·서식 전부 안내
- **미비 추가요청** — 접수에서 체크된 서류는 빼고, **남은 것만** 다시 요청 (예: 10종 중 4종 확인 → 6종만 요청). 다 받을 때까지 반복
- **AI로 자연스럽게** — `.env` API 키가 있으면 문장만 다듬음 (서류 목록·금액·사실은 바꾸지 않음)
- 발송 시 `.eml`(해당 서식 첨부) + ZIP 저장. 웹 Gmail은 자동 첨부가 안 되므로 `.eml` 또는 ZIP을 직접 첨부
- 화면 아래 **발송 이력**으로 몇 차까지 보냈는지 확인

### 4. 접수점검 (건수별)
- 왼쪽 **접수 건수 목록**에서 **일련번호**를 고른 뒤에만 파일을 올립니다. (선택 전 업로드 불가)
- 필터: 진행중 / 접수중 / 판정완료 / 전체 · 검색(일련번호·집단·비목)
- 체크·파일·메모·접수회차(`C-2026-0001-R01`)는 **그 건에만** 쌓입니다. 다른 건과 섞이지 않습니다.
- 특이점 메모 → 이 건 저장 또는 암묵지 축적
- 새 문의는 **새 일련번호**로만 생기고, 기존 건은 목록에 유지됩니다.

### 5. 집행판정
- 진행 / 보류 / 불가
- 예외·제외 서류는 **암묵지**로 쌓여 같은 사업 다음 상담에 반영

---

## 원칙
- OCR로 증빙 **자동 합격 하지 않음**
- 지침 문장 줄글은 서류 목록에 넣지 않음 (서식명만)
- 연습 도구(예시·시뮬레이션)는 1단계 **고급** 메뉴에 있음
