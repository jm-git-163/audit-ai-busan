# AuditAI Busan — 기술명세서 (예산 집행 상담·증빙 운영)

**버전:** 10.3  
**일자:** 2026-07-24  
**대상:** 공공기관(예: 국립대) 예산 담당자가 **기본 법령 + 사업 지침**을 기준으로 개별 집단의 집행 문의를 처리하고, 증빙 서식 안내·메일 발송·접수 점검·집행 가능 여부를 한 흐름으로 처리하는 시스템

### 10.3 현장 보완
- 요청 건별 서식 **점수 매칭 1:1 발췌** (동의어·부분일치)
- 고액·입찰·애매 건 **다음 조치 가이드** + 해결 사례 축적
- 사업별 상담 건 분리·전환(국립대↔RISE), 활성 건 영속화
- 증빙 OCR 자동합격 **비채택** (파일명 보조 + 담당자 체크)

---

## 1. 문제 정의

실무 흐름은 아래와 같다.

1. **기본 적용 법령**이 있다. (국가를 당사자로 하는 계약에 관한 법률 등)
2. **사업(예: 국립대 예산) 지침**이 내려온다.
3. **개별 집단**이 묻는다.  
   - 이 용도·계약이 가능한가?  
   - 비목은 무엇인가?  
   - 어떤 증빙이 필요한가?
4. 담당자는 **해당 비목 증빙 서식**을 안내하고, 집단 이메일로 **서식 파일**을 보낸다.
5. 증빙이 오면 **체크리스트**로 비목 요건 충족 여부를 검사한다.
6. 규정 위반 여부를 확인한 뒤 **예산 진행 여부(가능/보류/불가)** 를 확정한다.

기존 앱의 문제:

- 업로드·설정 창이 분산되어 같은 일을 여러 곳에서 하게 됨
- 질문 답변이 지침 원문·비목·증빙과 한 건으로 묶이지 않음
- 메일 전송·서식 첨부 흐름이 불명확
- 사전확인 / 증빙준비 / 사후감사가 분리되어 실무 한 건을 끝까지 못 따라감
- 불필요·중복 UI가 많음

---

## 2. 목표 프로세스 (5단계)

| 단계 | 이름 | 담당 작업 | 산출물 |
|------|------|-----------|--------|
| 1 | **법령·지침** | 기본 법령 ON/OFF, 사업 지침·증빙 서식 업로드(업로드는 **여기만**) | 적용 규정셋, 지침 문서함, 서식함 |
| 2 | **상담 답변** | 집단 질문 입력 → 비목·가능여부·필요증빙·지침 근거 문단 | **상담 건(Case)** |
| 3 | **증빙 요청** | 비목 매칭 서식 선택, 수신 이메일 입력 → 안내문+ZIP, 메일 발송 지원 | 요청 메일·서식 ZIP |
| 4 | **접수 점검** | 회신 증빙 파일 업로드 → 체크리스트 자동/수동 대조 | 구비율, 미비 목록 |
| 5 | **집행 판정** | 규정 스크리닝 + 증빙 완비 여부 → 진행/보류/불가 | 판정 기록·로그 |

한 상담 건이 2→3→4→5로 이어진다. 단계 이동 시 동일 Case를 유지한다.

---

## 3. 기능 요구사항

### 3.1 법령·지침 (Step 1)

- **기본 법령 패키지**(내장): 국가계약법·지방계약법·청탁금지법·세출집행기준 등 토글
- **사업 지침**: PDF/TXT 업로드 또는 붙여넣기 → IndexedDB 원문 저장 (파일당 ~200만 자)
- **증빙 서식**: HWP/PDF/XLSX 등 바이너리 IndexedDB 저장, 사업에 귀속
- 업로드 UI는 Step 1에만 존재 (다른 단계에 중복 업로드 버튼 금지)

### 3.2 상담 답변 (Step 2)

입력: 집단명(선택), 질문/상황 자유 텍스트, (선택) 금액·인원  
출력(한 화면에):

1. **비목 추천** (상위 후보 + 확정 선택)
2. **집행 가능 판정** (가능 / 조건부 / 정보부족 / 불가) + 규칙 근거
3. **필요 증빙 목록** (비목 표준 + 사업 지침 추출분)
4. **지침 근거 문단** (등록 문서 검색)
5. [상담 건으로 저장] → Case 생성 후 Step 3으로 이동 가능

### 3.3 증빙 요청 (Step 3)

- Case의 필요 증빙 ↔ 서식함 파일명 매칭
- 수신 이메일, 제목, 본문 편집
- **보내기**:  
  - 브라우저 한계상 SMTP 직접 발송 불가  
  - `mailto:`로 메일 클라이언트 오픈 + **서식 ZIP 자동 다운로드** (사용자가 ZIP 첨부)  
  - 본문·수신처 클립보드 복사 보조
- 발송 시각·대상 로그

### 3.4 접수 점검 (Step 4)

- Case 선택 후 회신 파일 일괄 업로드 (파일명·내용 힌트로 항목 매칭)
- 체크리스트: 필요 서류별 ☐/☑ + 매칭 파일명
- 미비 항목 재요청 문구 생성

### 3.5 집행 판정 (Step 5)

- 기존 규칙 엔진(한도·금지업종·심야·계약방식·사업지침 제한 등) 재사용
- 증빙 완비율 반영
- 최종: **진행 가능 / 보류(보완) / 불가** + 담당자 메모
- 감사 로그 기록

---

## 4. 비기능 요구

| 항목 | 내용 |
|------|------|
| 실행 | 단일 `index.html` + CDN(PDF.js). 서버 불필요 |
| 저장 | localStorage(설정·건 메타) + IndexedDB(지침 원문·서식 바이너리) |
| 메일 | mailto + ZIP 첨부 안내 (서버 SMTP는 후속) |
| 보안 | 문서·개인정보는 브라우저 로컬. 외부 AI 전송 없음 |
| UX | 상단 5단계만. 중복 업로드·중복 확인 폼 제거 |

---

## 5. 데이터 모델 (요약)

```
Project { id, name, fund, period, budget, ov, ... }
GuideDoc { id, name, prj, text, chars, at }          // IndexedDB
FormFile { id, name, prj, data, size }               // IndexedDB
Case {
  id, group, email, q, cat, amount, people,
  status: draft|asked|requested|checking|decided,
  needDocs[],       // 필요 증빙 명칭
  formIds[],        // 매칭 서식
  received[{name, matchedDoc}],
  checks{ docName: bool },
  verdict: null|proceed|hold|reject,
  reasons[], memo, at, decidedAt
}
```

---

## 6. UI 정보 구조

```
[헤더] AuditAI · 사업 선택 · 규정
[스텝바] 1법령·지침 | 2상담 | 3증빙요청 | 4접수점검 | 5집행판정

Step1: 기본법령 토글 | 지침 업로드 1곳 | 서식 업로드 1곳
Step2: 질문 입력 → 답변 카드 → [건 저장·증빙요청으로]
Step3: 건 선택 | 서식 목록 | 이메일 | [ZIP+메일]
Step4: 건 선택 | 파일 드롭 | 체크리스트
Step5: 건 선택 | 판정 요약 | 진행/보류/불가
```

제거·축소: 분산된 “지침 PDF 올리기”, 이중 사전확인 폼, 독립 FAQ 대형 패널(검색은 Step2에 통합), 거래처 대장·PII 도구는 설정 서랍으로 이동, 사후감사 CSV 대량은 Step5 보조로만 유지.

---

## 7. 구현 우선순위 (본 버전)

1. 5단계 UI 골격 + 업로드 단일화  
2. Case 생명주기 (2→5)  
3. 상담 답변 = 규칙엔진 + 지침검색 + 증빙목록  
4. 서식 매칭 + mailto/ZIP  
5. 접수 체크리스트 + 최종 판정  

---

## 8. 성공 기준

- 담당자가 **업로드를 Step1에서만** 수행  
- 집단 질문 1건으로 **비목·가능여부·증빙·근거**를 한 화면에서 확인  
- 이메일 입력 후 **안내용 메일 창 + 서식 ZIP**이 바로 나옴  
- 회신 파일로 **체크리스트 완료율**이 보이고  
- **진행/보류/불가**를 건 단위로 남길 수 있음  
